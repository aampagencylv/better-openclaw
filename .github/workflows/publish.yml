name: Publish

on:
  release:
    types: [published]

permissions:
  contents: read
  id-token: write

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
          registry-url: "https://registry.npmjs.org"

      - run: pnpm install --frozen-lockfile

      - run: pnpm lint --filter @better-openclaw/cli

      - run: pnpm typecheck --filter @better-openclaw/cli

      - run: pnpm test --filter @better-openclaw/cli

      - run: pnpm build --filter @better-openclaw/cli

      - name: Publish CLI to npm
        run: pnpm publish --filter @better-openclaw/cli --no-git-checks --access public
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
