---
name: code-server-develop
description: "Run VS Code in the browser using code-server at {{CODE_SERVER_HOST}}:{{CODE_SERVER_PORT}}."
metadata:
  openclaw:
    emoji: "💻"
---

# Code Server Development

code-server is available at `http://{{CODE_SERVER_HOST}}:{{CODE_SERVER_PORT}}` within the Docker network.

## Tips for AI Agents

- Full VS Code experience accessible via any web browser.
- Install extensions from the marketplace for language support.
- Use the integrated terminal for command-line operations.
- Projects persist in the mounted workspace volume.
